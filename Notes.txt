Steps for pushing to Git

1) git status = identifies changes to files
2) git add -A = adds all files
3) git commit -m "Changes that were made." = commits the files for uploading
4) git push origin master = pushes committed files to my GitHub

Gulp & Node.js command prompt

1) cd (drag travel-site folder here)
2) gulp watch = turns on 
3) CTRL + C = turns off

BEM = Block Element Modifier
	Define using class
	Block = large-hero
	Element = large-hero__title
	Modifier = btn--orange
	
